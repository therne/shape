
import com.therne.app.models.User
import theirTheme.shs

use rtl
use binding {
    class .app.bindings.UserDetailsBinding
    param user User
    param isFullLayout Boolean
}

Linear (width: match, height: match) {
    Text (text: "Hello world!")

}
